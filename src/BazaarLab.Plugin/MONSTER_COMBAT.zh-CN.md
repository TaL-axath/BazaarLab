# 野怪战斗计算 v1.0.3

在游戏选中野怪、野怪棋盘加载完成且战斗尚未开始时，系统会在野怪棋盘稳定 1 秒后
自动计算，并把胜率悬浮显示在野怪头像上方。

计算输入直接来自游戏当前权威状态：

- 玩家物品、技能、英雄属性与当前生命；
- 野怪物品、技能、英雄类型、生命及其他战斗属性；
- 物品实例 ID、模板 ID、品质、附魔、尺寸、槽位、标签和运行时属性。

计算开始时会冻结 `monster-input-时间戳.json`，避免实时导出在 Monte Carlo 运行期间
改变输入。独立战斗进程固定执行 50 场，最多使用 8 路并发；每场使用独立且确定的
run index，单场最多模拟 2400 tick。

头像标签显示：

- 玩家实际胜率；
- 玩家胜 / 野怪胜 / 平局次数；
- 固定样本数 50。

如果计算完成前玩家、野怪、时间或遭遇发生变化，结果会标记为正在更新。战斗开始后
计算自动停止，因为此时结果已不再能用于战前决策。

输出位于 `BepInEx/config/BazaarLab/`：

- `monster-input-*.json`：不可变计算输入；
- `monster-result-*.json`：完整预测结果。
