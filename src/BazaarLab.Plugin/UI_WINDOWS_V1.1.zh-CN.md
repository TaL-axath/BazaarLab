# v1.1 界面调整

- 删除旧的 `Monster Combat / CALCULATE` 手动野怪计算面板。
- 保留野怪候选卡自动计算与结果覆盖显示。
- 保留后台兼容计算和已经存在的野怪头顶结果。
- `Local Placement` 摆位窗口可通过标题栏拖动。
- `30s Infinite Whiteboard` 曲线窗口可通过标题栏拖动。
- 两个窗口均可用右上角 `—` 最小化，并用 `+` 恢复。
- 窗口位置受屏幕边界保护，即使向屏幕外拖动也会保留可找回的标题区域。

窗口的位置与折叠状态在当前游戏进程中保持；重启游戏后恢复默认布局。
